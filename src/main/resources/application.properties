spring.application.name=desk-collection

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/desk_collection
spring.datasource.username=postgres
spring.datasource.password=password
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.database=POSTGRESQL
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Flyway
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true

# Thymeleaf
spring.thymeleaf.cache=false

# Security (Allow everything for demo purposes, or configure basic auth)
# For simplicity in this demo, we might want to disable CSRF for the API if we use simple JS calls
# But since we have spring-security, it will secure by default.
# I'll add a SecurityConfig to allow public access to the API and UI for this demo assignment.
